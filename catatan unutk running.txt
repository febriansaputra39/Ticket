Jika kalau mau running buka di terminal, tetapi itu jika sudah expor data sql nya di phpyadmin dan juga running backend nya, kalau belum running backend nya, masukkan perintah: npm install, jika sudah install, npm run dev/ npm server.js jika tidak bisa running npm run dev nya, habis, ulangi di bagian frontend nya, tetapi beda nya tidak menggunakan npm run server.js, bisa langsung npm run dev nya